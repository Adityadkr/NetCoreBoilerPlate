
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>



<table id="tblAliens" class="table table-responsive table-bordered" width="100%">
    <thead>
        <tr>
            <td>name</td>
            <td>pass</td>
            @*<td>Edit</td>
            <td>View</td>*@

        </tr>
    </thead>
</table>
<script>



    $(document).ready(function () {

        var column = [
            { "data": "name", "name": "name" },
            {
                data: "pass", render: function (data, type, row) {

                    return '<span class="text-danger">' + data + '</span>';
                }
            },
            {
                "data": "name ", "name": "Edit", render: function (data, type, row) {

                    return '<button class="btn btn-primary">Edit</span>';
                }, "autoWidth": true
            },
            {
                "data": "name", "name": "View", render: function (data, type, row) {


                    return '<button class="btn btn-success">View</span>';
                }, "autoWidth": true
            }

        ]


        $.ajax({
            url: "/Demo/GetAliens",
            type: "GET",
            dataType: 'json',
            success: function (resp) {
                debugger;
                   
                CreateCommonGridWithData("#tblAliens", resp, column)
            }
        })

        //var column = [
        //            { "data": "name", "name": "name"},
        //            {
        //                data: "pass", render: function (data, type, row) {

        //                    return '<span class="text-danger">' + data + '</span>';
        //                }
        //            },
        //            {
        //                "data": "name ", "name": "Edit", render: function (data, type, row) {

        //                    return '<button class="btn btn-primary">Edit</span>';
        //                }, "autoWidth": true
        //            },
        //            {
        //                "data": "name", "name": "View", render: function (data, type, row) {


        //                    return '<button class="btn btn-success">View</span>';
        //                }, "autoWidth": true
        //            }

        //        ]
        //CreateCommonGridAjax("#tblAliens", "/Demo/GetAliens",column)
      
    })
</script>